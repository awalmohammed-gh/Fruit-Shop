import { useState } from "react";
import { NavLink } from "react-router-dom";
import {Search,ShoppingBag, Menu} from "lucide-react"
import { useAppContext } from "../Context/FruitContextApi";

const Navbar = () => {
     const [open, setOpen] = useState(false);
     const {user, setUser} = useAppContext();


  return (
    <div>
      <nav className="flex items-center justify-between px-6 md:px-16 lg:px-24 xl:px-32 py-4 border-b border-gray-300 bg-white relative transition-all">
        <NavLink>
          <h2 className="text-xl sm:text-4xl font-bold">
            Fruit.<span className="text-primary">Bay</span>
          </h2>
        </NavLink>

        {/* Desktop Menu */}
        <div className="hidden sm:flex items-center gap-12">
          <NavLink className={`text-xl tracking-wide cursor-pointer`} to={"/"}>Home</NavLink>
          <NavLink className={`text-xl tracking-wide cursor-pointer`} to={"/"}>Products</NavLink>
          <NavLink className={`text-xl tracking-wide cursor-pointer`} to={"/"}>Contact</NavLink>

          <div className="hidden lg:flex items-center text-sm gap-2 border border-gray-300 px-3 rounded-full">
            <input
              className="py-1.5 w-full bg-transparent outline-none placeholder-gray-500"
              type="text"
              placeholder="Search products"
            />
            <Search/>
          </div>

          <div className="relative cursor-pointer">
            <ShoppingBag/>
            <button className="absolute -top-2 -right-3 text-xs text-white bg-primary w-[18px] h-[18px] rounded-full">
              3
            </button>
          </div>

          <button className="cursor-pointer px-8 py-2 bg-primary hover:bg-primary-dull transition text-white rounded-full">
            Login
          </button>
        </div>

        <button
          onClick={() => (open ? setOpen(false) : setOpen(true))}
          aria-label="Menu"
          className="sm:hidden"
        >
          {/* Menu Icon SVG */}
          <Menu/>
        </button>

        {/* Mobile Menu */}
        <div
          className={`${
            open ? "flex" : "hidden"
          } absolute top-[60px] left-0 text-lg w-full bg-white shadow-md py-4 flex-col items-start gap-2 px-5 text-sm md:hidden`}
        >
          <NavLink onClick={() => setOpen(false)} to={"/"} className="block">
            Home
          </NavLink>
          <NavLink onClick={() => setOpen(false)} to={"/"} className="block">
            Products
          </NavLink>
          <NavLink onClick={() => setOpen(false)} to={"/"} className="block">
            Contact
          </NavLink>
          <button className="cursor-pointer px-6 py-2 mt-2 bg-primary hover:bg-primary-dull transition text-white rounded-full text-sm">
            Login
          </button>
        </div>
      </nav>
    </div>
  );
}

export default Navbar
